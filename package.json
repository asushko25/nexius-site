{
  "name": "nexius",
  "version": "1.0.0",
  "description": "Swivl test task",
  "main": "index.js",
  "scripts": {
    "build-css": "sass src/styles/styles.scss src/styles/styles.css --watch",
    "predeploy": "sass src/styles/styles.scss src/styles/styles.css && mkdir -p dist && cp -r src/* dist/",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.80.4",
    "stylelint": "^16.12.0",
    "stylelint-scss": "^6.10.0"
  }
}
